POSTGRES_URL=
# Optional fallback if your provider exposes DATABASE_URL instead of POSTGRES_URL.
DATABASE_URL=

# Required in production if atelier users should upload custom lookbook images.
BLOB_READ_WRITE_TOKEN=
